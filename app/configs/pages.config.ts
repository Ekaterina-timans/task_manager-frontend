class DASHBOARD {
	private root = '/'
	STATISTICS = this.root
	COMMON_TASKS = `${this.root}/common-tasks`
	PERSONAL_TASKS = `${this.root}/personal-tasks`
	CHAT = `${this.root}/chat`
    SETTINGS = `${this.root}/settings`
    ADMIN_PANEL = `${this.root}/admin-panel`
}

export const DASHBOARD_PAGES = new DASHBOARD()